package ir.modernshadow.bladeclient.visual;

public final class NightVisionModule {
    private NightVisionModule() {}

    public static void register() {
        // Intentionally empty: Night vision is applied via lightmap mixin (client-side only).
    }
}
